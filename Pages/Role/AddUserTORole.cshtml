@page "{Id}"
@model MTLcourts.Pages.AddUserTORoleModel
@{
}


<h1>Users not in this Role : @Model.Role.Name</h1>
<form method="post">
    <table class="table table-sm table-bordered table-bordered">
        <tr>
            <th class="col-1">IsSelected</th>
            <th class="col-4">UserID</th>
            <th class="col-3">UserName</th>
            <th class="col-3">Email</th>
            
        </tr>
        @foreach (var user in Model.userRole)
            {
                <tr>
                    <td class="col-1">
                        <input type="checkbox" name="IsSelected" asp-for="@user.IsSelected"/>
                    </td>
                    <td class="col-4">@user.UserId</td>
                    <td class="col-3">@user.UserName</td>
                    <td class="col-3">@user.UserEmail</td>
                </tr>    
            @* <div class="form-group row">
            <label asp-for="@user.IsSelected" class="col-1" ></label>
            <label asp-for="@user.UserId" class=" col-5"></label>
            </div>
            <div class="form-group row">
            <input type="checkbox" asp-for="@user.IsSelected" class="col-1" />
            <input asp-for="@user.UserId" class=" col-5" />     
            </div> *@
        
            }
                    
    </table> 

    


    <button type="submit" class="btn btn-primary col-2" >update</button>
    <th scope="col"></th><a asp-page="AdminManagement"  class="btn btn-primary col-2">Cancel</a></th>
    </form>
